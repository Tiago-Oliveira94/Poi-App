<h1>New POI</h1>


<%= form_with scope: :poi, url: pois_path, local: true do |form| %>

 <% if @poi.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@poi.errors.count, "error") %> prohibited
        this point_of_interest from being saved:
      </h2>
      <ul>
        <% @poi.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.label :x %><br>
    <%= form.number_field :x %>
  </p>

    <p>
    <%= form.label :y %><br>
    <%= form.number_field :y %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to 'Back To List', pois_path %>